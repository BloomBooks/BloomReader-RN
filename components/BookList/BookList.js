import React from "react";
import { SafeAreaView, Text } from "react-native";

export default class BookList extends React.PureComponent {
  render() {
    return (
      <SafeAreaView>
        <Text>This is the Book List</Text>
      </SafeAreaView>
    );
  }
}
