import React from "react";
import { SafeAreaView, Text } from "react-native";

export default class BookReader extends React.PureComponent {
  render() {
    return (
      <SafeAreaView>
        <Text>This is the Book Reader. Put a Bloom Player here vvv</Text>
      </SafeAreaView>
    );
  }
}
